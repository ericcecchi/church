<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "church", root_path, :class => 'brand' %>
      <div class="nav-collapse">
        <ul class="nav">
          <li><%= link_to "home", root_path %></li>
          <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" 
              class="dropdown-toggle"
              data-toggle="dropdown">
              internal
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
                  <li><%= link_to "rails", internal_rails_path %></li>
                  <li><%= link_to "admin dash", internal_admin_dash_path %></li>
            </ul>
            </li>
          <% end %>
        </ul>
        <ul class="nav pull-right">
          <% if user_signed_in? %>
          <li><%= link_to current_user.username, short_profile_path(current_user.username) %></li>
          <li class="divider-vertical"></li>
          <li><%= link_to "sign out", destroy_user_session_path, :method => :delete %></li>
          <% else %>
          <li><%= link_to "sign in", authentications_path %></li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse --> 
    </div>
  </div>
</div>