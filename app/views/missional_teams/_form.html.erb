<%= form_for(@missional_team) do |f| %>
  <% if @missional_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@missional_team.errors.count, "error") %> prohibited this missional_team from being saved:</h2>

      <ul>
      <% @missional_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
