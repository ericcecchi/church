<%= form_for(@service_sermon) do |f| %>
  <% if @service_sermon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_sermon.errors.count, "error") %> prohibited this service_sermon from being saved:</h2>

      <ul>
      <% @service_sermon.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
